
  # Slice

  This is a code bundle for Slice. The original project is available at https://www.figma.com/design/R8ZMqqULlM10pOhdJBcgsl/Slice.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  